package com.example.nbaapp.utils

/**
 * Enum class to include two View types for adapters. The [HEADER] with ordinal 0 denotes a header
 * view type, while the [ITEM] with ordinal 1 denots an item view type for the adapter.
 */
enum class ViewType {
    HEADER,
    ITEM
}